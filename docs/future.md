# Future ideas (not in v1)

- Additional skills (e.g. SQL review, data-pipeline triage) once one person owns the contract.
- Optional web observability, subject to your org’s policy, behind feature flags.
- Tighter merge helpers for `hooks.json` if you run multiple hook bundles in one repo.

Add issues in your fork when you promote an item from this list to work.
