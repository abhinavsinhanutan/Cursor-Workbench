import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://abhinavsinhanutan.github.io',
  base: '/Cursor-Workbench',
  outDir: '../docs',
  trailingSlash: 'ignore',
});
